quarkus.http.host=0.0.0.0
quarkus.http.port=${PORT:8080}
quarkus.http.proxy.proxy-address-forwarding=true
quarkus.http.static-resources.index-page=index.html
quarkus.jackson.fail-on-unknown-properties=false
quarkus.console.color=true
quarkus.log.console.darken=0
quarkus.log.category."dev.huff.hexaquot.api.ApiRequestLoggingFilter".level=INFO
app.auth.enabled=${AUTH_ENABLED:true}
app.words.path=${WORDS_PATH:words/it-words.json}
app.game.timezone=${GAME_TIMEZONE:Europe/Rome}
app.word.seed=${HUFF_WORD_SEED:huff-it-hexaquott}
app.hexahack.seed=${HUFF_HEXAHACK_SEED:huff-it-hexaquott-hexahack}
app.hexasky.seed=${HUFF_HEXASKY_SEED:huff-it-hexaquott-hexasky}
app.cookie.secure=${COOKIE_SECURE:true}
app.push.vapid.public-key=${VAPID_PUBLIC_KEY:}
app.push.vapid.private-key=${VAPID_PRIVATE_KEY:}
app.push.vapid.subject=${VAPID_SUBJECT:mailto:notifications@hexaquot.it}
app.push.new-game.cron=${PUSH_NEW_GAME_CRON:0 31 0 * * ?}
app.push.daily-reminder.cron=${PUSH_DAILY_REMINDER_CRON:0 0 23 * * ?}
app.push.weekly-awards-reminder.cron=${PUSH_WEEKLY_AWARDS_REMINDER_CRON:0 10 20 ? * MON}
app.push.hexahack-launch.enabled=${PUSH_HEXAHACK_LAUNCH_ENABLED:false}
app.push.hexasky-launch.enabled=${HEXASKY_LAUNCH:false}
app.leaderboard.weekly-awards.cron=${LEADERBOARD_WEEKLY_AWARDS_CRON:0 5 0 ? * MON}

quarkus.datasource.db-kind=postgresql
quarkus.datasource.devservices.enabled=false
quarkus.datasource.jdbc.min-size=1
quarkus.datasource.jdbc.max-size=5
# Flyway owns the schema. Hibernate only verifies that the mapped model and the
# versioned migrations stay aligned.
quarkus.hibernate-orm.database.generation=validate
quarkus.flyway.migrate-at-start=true
# Existing installations were created by Hibernate and the legacy scripts.
# Baseline them at 0 so the idempotent historical migrations are recorded and
# applied once without losing data.
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.baseline-version=0
quarkus.flyway.placeholders.game_timezone=${GAME_TIMEZONE:Europe/Rome}
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:huff_hexaquot}
%dev.quarkus.datasource.username=${POSTGRES_USER:huff}
%dev.quarkus.datasource.password=${POSTGRES_PASSWORD:huff}
%prod.quarkus.datasource.jdbc.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:huff_hexaquot}
%prod.quarkus.datasource.username=${POSTGRES_USER:huff}
%prod.quarkus.datasource.password=${POSTGRES_PASSWORD:huff}

quarkus.oidc.enabled=true
quarkus.oidc.tenant-enabled=${AUTH_ENABLED:true}
# Quarkus validates the initial Authorization: Bearer <token>. The application
# then creates its own persistent, revocable HttpOnly session cookie.
quarkus.oidc.application-type=service
quarkus.oidc.provider=google
quarkus.oidc.client-id=${GOOGLE_CLIENT_ID:}
# Google browser access tokens are opaque: validate them using Google's
# UserInfo endpoint instead of treating them as JWT ID tokens.
quarkus.oidc.token.verify-access-token-with-user-info=true
%dev.app.auth.enabled=${AUTH_ENABLED:false}
%dev.app.cookie.secure=${COOKIE_SECURE:false}
%dev.quarkus.oidc.tenant-enabled=${AUTH_ENABLED:false}

%test.quarkus.datasource.devservices.enabled=true
%test.quarkus.datasource.devservices.db-name=huff_hexaquot_test
%test.quarkus.datasource.devservices.username=huff
%test.quarkus.datasource.devservices.password=huff
%test.quarkus.hibernate-orm.database.generation=validate
%test.app.auth.enabled=false
%test.app.cookie.secure=false
%test.app.push.vapid.public-key=
%test.app.push.vapid.private-key=
%test.quarkus.oidc.enabled=false
