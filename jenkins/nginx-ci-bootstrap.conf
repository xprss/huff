server {
    listen 80;
    listen [::]:80;
    server_name ci.ottonovembre.it;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    location / {
        return 503;
    }
}
